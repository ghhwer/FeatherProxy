# Copy to .env and edit as needed.
# MESSAGE is printed when the app runs.
MESSAGE=Hello, World!

# Database (ORM). Use either postgres or sqlite.
# PostgreSQL example:
# DB_DRIVER=postgres
# DB_DSN=host=localhost user=feather password=secret dbname=featherproxy port=5432 sslmode=disable
# SQLite example (file):
DB_DRIVER=sqlite
DB_DSN=file:data.db

# Authentication token encryption (required if using authentications). Use at least 32 bytes, e.g. base64:
# AUTH_ENCRYPTION_KEY=$(openssl rand -base64 32)
AUTH_ENCRYPTION_KEY=

# Caching strategy (none, memory, redis).
# CACHING_STRATEGY=none
CACHING_STRATEGY=memory

# Redis configuration (required if caching strategy is redis).
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

#REDIS_HOST=localhost
#REDIS_PORT=6379
#REDIS_PASSWORD=
#REDIS_DB=0